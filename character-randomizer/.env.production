# Public URL of the Firebase Hosting "data" site (see firebase.json / .firebaserc
# hosting target "data"). Not a secret - just the endpoint the app fetches
# app-data.json and character images from at runtime.
VITE_DATA_BASE_URL=https://character-randomizer-data.web.app
